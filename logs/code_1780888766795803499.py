import os from pathlib import Path # Get the Downloads folder path based on the operating system downloads_path = Path.home() / "Downloads" # Define the file name and full path file_name = "txt.txt" file_path = downloads_path / file_name # Content to save in the file content = "Hello, this is some content to save!" # Create the Downloads directory if it doesn't exist downloads_path.mkdir(parents=True, exist_ok=True) # Write content to the file with open(file_path, "w") as file: file.write(content) print(f"✅ File saved successfully at: {file_path}")
